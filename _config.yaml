
title: Demiurge.JS
description: A lightweight architecture for static sites that behave like a SPA: internal routing, layout partials, per-page scripts, and an accessible modal service.
show_downloads: false
remote_theme: pages-themes/hacker@v0.2.0
plugins:
  - jekyll-remote-theme # add this line to the plugins list if you already have one
